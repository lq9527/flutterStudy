# JIT 和 AOT
# 有状态和无状态 
# 动画
# Page对应的Activity
# 国际化（多语言支持）
# 布局、盒约束（线性，相对
# 手势，动画
# 显示java的自定义View
# Render Object
    Widget：对应React.ReactElement。
    State：对应React.Component。
    Render Object：对应HTML或者CSS。

# InheritedWidget
# 通知
# 复用FlutterView
# flutter 线程是隔离的。通信？
# Dart 与 JavaScript 一样，是单线程的
# RetryNotification().dispatch(context);
# 高阶组件


# 创建一个组件，放到仓库




### 开源项目
    dio、fly、dsBridge、

